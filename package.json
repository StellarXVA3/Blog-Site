{
  "name": "blog-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass:watch": "sass --watch src/sass:src",
    "sass:build": "sass src/sass:public",
    "postcss:build": "postcss src/style.css --dir public",
    "postcss:watch": "postcss --watch src/style.css --dir src"
  },
  "devDependencies": {
    "cssnano": "^7.0.7",
    "postcss": "^8.5.4",
    "postcss-mq-optimize": "^2.0.1",
    "sass": "^1.89.1",
    "vite": "^6.3.5"
  }
}
